services:
  - type: web
    name: checkout-woovi-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: WOOVI_APP_ID
        sync: false
      - key: PORT
        value: "3000"
